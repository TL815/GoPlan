name: goplan_agent_prototype
description: Standalone prototype for the GoPlan travel AI agent.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.5.0

dependencies:

dev_dependencies:
  lints: ^4.0.0
  test: ^1.25.0
